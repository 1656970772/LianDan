# M4 最终浏览器复审报告

| 字段 | 值 |
| --- | --- |
| 日期 | 2026-07-15 |
| 应用地址 | `http://127.0.0.1:4187/` |
| 会话 | `liandan-m4-final` |
| 视口 | 1600×1200 |
| 范围 | M4 完整内容、背包标签 Tips、多药材选择与投入、基础可访问性、控制台与资源错误 |

## 摘要

| 严重度 | 数量 |
| --- | ---: |
| 严重 | 0 |
| 高 | 0 |
| 中 | 0 |
| 低 | 0 |
| 合计 | 0 |

本轮真实浏览器复审未发现新的可复现产品缺陷。

## 验收证据

- 冷启动可见 8 个正式药材批次，名称、保存状态、来源、年份和剩余份数均可访问。
- 凡火可装备；药材可选择并投入，赤须参剩余份数由 3 正确降为 2；随后可切换选择寒髓晶。
- 赤须参与寒髓晶 Tips 均显示药性、功效线索、反应特征、风险、批次状态 5 类标签；每个批次共显示 7 个强度条。
- 交互过程中未发现页面错误、失败资源请求或应用控制台异常；控制台仅有 Vite 开发连接调试信息。
- 主要交互元素均暴露明确的 button、radio、slider、checkbox、tooltip 与 meter 语义。

## 正式产物

- `screenshots/M4最终复审-1600x1200.png`：完整界面、8 批次背包区域、赤须参五类 Tips 与待投状态。
- `screenshots/M4最终复审-1600x900.png`：900 像素高视口的补充检查截图。

## 自动化复核

- `npm run check`：43 个测试文件、468 项测试通过；类型检查、配置、8+8 素材校验和生产构建通过。
- `npm run test:e2e`：最终隔离端口复跑 Chrome 与 Edge 共 74/74 通过。
- 第一次全量 E2E 的既有 M1 `crowd` 性能探针在 12 worker 负载下记录到 3 个 dropped tick；该探针随后在独立端口并发重复 20/20 通过，完整 E2E 再跑 74/74 通过。M4 用例在两次全量运行中均通过。

## 环境说明

真实浏览器复审使用独立端口 4187，自动化复核使用独立端口 4191–4193，避免共享 4173 端口上的并发开发服务干扰。环境未安装 `ffmpeg`，因此没有生成视频；验收证据由 DOM/可访问性快照、控制台与错误检查、PNG 截图和 Playwright E2E 共同组成。
