# 项目规则

## 项目目的

本项目旨在开发一个网页端的炼丹规则开发调试工具。它服务于规则设计与数值验证，不是玩家最终游玩的炼丹小游戏。网页端用于调整药材与炼制因素，并以一次无状态推演直接得到最终丹药结果；稳定后的配置、规则说明和确定性测试案例将迁移到 Unity。

网页端的核心目标如下：

- 药材、材料状态、投料信息、炼制条件和其他规则输入均通过配置定义，滑动条、下拉选择、数字框和开关等控件必须由因素元数据动态生成；新增普通因素不得修改页面组件代码；
- 用户调整输入后可以主动执行成丹推演，查看最终是否成丹，以及丹药名称、品质、数量、功效、副作用、特殊特质和评价等结果；
- 调试工具不展示逐帧炼制过程或反应时间线，但必须展示可追溯的最终判定依据摘要，包括命中的身份条件、未满足的候选条件、决定性正负因素、品质原因、结果修正来源和失败原因；
- 不同材料和条件组合应当能够产生不同的丹药与效果，同时通过明确、确定、可配置和可测试的规则把结果控制在设计范围内；
- 丹药采用受控动态生成：配置化丹药原型决定身份、正式名称和基础功效，配置化结果修正规则增删或调整次要功效、副作用、特殊特质及参数；禁止自由文本命名、生成式 AI 决策和无界属性累加；
- 材料、标签、反应、成丹条件、品质阈值和结果分支等领域规则不得针对单个测试场景硬编码，应沉淀为可复用的配置结构和纯规则模块；
- 同一配置版本、同一输入快照和同一随机种子必须得到完全相同的结果；规则冲突和非法配置必须明确报错，不得静默选择或回退；
- 网页版本验证稳定后，将规则配置、规则说明和确定性测试案例迁移到 Unity，作为正式版本炼丹系统的实现依据。

## 网页端边界

- 页面主流程固定为“选择药材与顺序 → 调整因素 → 点击推演成丹 → 查看最终结果与原因摘要”。
- 不实现火焰跟随控制、药材局部溶解、接液、丹珠物理、碰撞、计时状态机、过程回放或其他实时玩法。
- 不使用 Phaser、物理引擎或后端服务；第一版为 Vite、React、TypeScript 驱动的本地单页工具。
- 萃取、融合、凝丹等词只可作为输入因素或规则语义，不得演变为需要用户逐阶段操作的流程。
- 成功、未成丹、废丹、残丹、异丹和炸炉都是领域结果；配置错误与执行异常才是技术错误。

## 内容与世界观规则

- 药材、丹药、炼药师和天地灵物可参考项目指定的小说分析报告，但报告事实、报告中的歧义与项目补全设计必须在文档中区分。
- 修仙语义必须服务于可解释规则。名称、描述、功效和副作用可以有小说感，但不得以玄学措辞替代明确条件、数值范围和原因码。
- 材料状态、来源和年龄名称必须按类别配置并校验；魔核不得使用“新鲜、干燥、药圃、年份”等灵植语义，应使用完整度、妖兽或秘境来源与兽龄等对应表达。
- 初版内容优先覆盖具有代表性的聚气、回气、疗伤、护体、清魂、爆发和特殊状态丹药，并确保主要成功与失败分支都有确定性预设可达。

## GitHub 提交规则

- 每个开发阶段完成并通过验证后，必须将该阶段的代码和相关文档提交到 GitHub。
